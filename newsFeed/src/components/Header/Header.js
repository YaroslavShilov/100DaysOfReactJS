import React from 'react';
import './Header.css';

const Header = (props) => {
	return (
		<div className={'header d-flex'}>
			<h1>@NickName</h1>
			<h2>all 5, like 0</h2>
			
		</div>
	);
}

export default Header